# Task kickoff: Align README payload wording with NE schema

Issue: #456

Plan:

- Update README NE schema section: `payload` is object|array.
- Add validation note: drop `composed`; `composed[].payload` may be object|array|null.
- Keep edits minimal and scoped.
