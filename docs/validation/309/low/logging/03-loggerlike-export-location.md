# Decide export location for LoggerLike

Document where `LoggerLike` and related types will live (e.g., `src/log.ts`
or a dedicated `src/types/log.ts`) to avoid duplication and make import paths
clear for consumers.
