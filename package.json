{
  "name": "@a5c-ai/events",
  "version": "0.0.0",
  "private": true,
  "description": "SDK and CLI scaffolding for events parsing/enrichment (a5c)",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "preinstall": "node -e \"console.log('Using Node', process.version)\"",
    "prepare": "husky install || echo 'husky skipped'",
    "build": "echo 'build: nothing to build yet'",
    "test": "node -e \"console.log('ok: placeholder tests')\"",
    "lint": "eslint . || echo 'eslint not configured yet'",
    "release": "echo 'release: disabled for scaffolding'"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^9.1.5",
    "eslint": "^9.10.0"
  }
}
